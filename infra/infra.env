export AWS_REGION=$(aws configure get region)
export ACCOUNT_ID=$(aws sts get-caller-identity --query "Account" --output text)
export GITHUB_REPO=mbimbij/kafka-platform
export BASTION_HOST_KEY_NAME=t480
